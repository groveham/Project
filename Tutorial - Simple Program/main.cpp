#include <iostream>

using namespace std;

int main()
{
    cout << "Hello world!" << endl; //endl means end line, and move onto next. \n Also does the same, it just means go to a new line.
    cout << "And everyone else! \n" ; // cout mean computer out, or print out. Displays the text in the brackets. The << means run the command with what ever is next to it
    cout << "This is on a new line! \n" ;  // for example, cout << "and everyone else!"; means, put "and eveyone else!" into the cout command and do that command.
    return 0;
}
